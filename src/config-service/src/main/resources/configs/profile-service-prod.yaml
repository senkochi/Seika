spring:
  cache:
    type: redis
  data:
    redis:
      password: ${REDIS_PASSWORD}
  jpa:
    show-sql: false

server:
  port: ${PROFILE_SERVER_PORT:8082}

logging:
  level:
    root: WARN
    com.seika: INFO

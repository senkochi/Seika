spring:
  cache:
    type: redis
  data:
    redis:
      password: ${REDIS_PASSWORD}
  jpa:
    show-sql: false

server:
  port: ${MARKETPLACE_SERVER_PORT:8085}

logging:
  level:
    root: WARN
    com.seika: INFO

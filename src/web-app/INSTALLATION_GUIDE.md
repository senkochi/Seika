## NOTE

Dùng `npm install react-day-picker --legacy-peer-deps` thay vì `npm install` để tránh lỗi peer dependencies khi cài đặt nha.

## REACTBIT INSTALLATION GUIDE

Thêm `--path src/components/reactbit` vào lệnh `npx reactbit add` để cài đặt ReactBit vào đúng thư mục.

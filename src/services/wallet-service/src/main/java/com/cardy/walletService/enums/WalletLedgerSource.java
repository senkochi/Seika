package com.cardy.walletService.enums;

public enum WalletLedgerSource {
    PAID,
    BONUS,
    REWARD,
    EARNED_WITHDRAWABLE,
    EARNED_PROMO,
    PLATFORM_FEE_REAL,
    PLATFORM_FEE_PROMO_SINK
}

spring:
  config:
    import: "configserver:"
  application:
    name: profile-service
  profiles:
    active: ${SPRING_PROFILE:dev}
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL}
      fail-fast: true
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6

# Các cấu hình datasource, eureka... được định nghĩa trong Config Server
# Xem /config-service/src/main/resources/configs/profile-service.yaml

# Các cấu hình datasource, JWT, Eureka... được định nghĩa trong Config Server
# Xem /config-service/src/main/resources/configs/profile-service.yaml

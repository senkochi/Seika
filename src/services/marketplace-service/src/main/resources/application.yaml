spring:
  application:
    name: marketplace-service
  datasource:
    url: jdbc:postgresql://localhost:5432/marketplace-service-seika
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

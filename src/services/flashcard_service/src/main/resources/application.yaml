spring:
  config:
    import: "configserver:"
  application:
    name: flashcard-service
  profiles:
    active: ${SPRING_PROFILE:dev}
  data:
    mongodb:
      uri: mongodb+srv://senko:123@clustercard.bdhmypa.mongodb.net/flashcard_db?retryWrites=true&w=majority
      socket-timeout-ms: 30000
      connection-timeout-ms: 30000
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      fail-fast: false
      retry:
        initial-interval: 1000
        max-interval: 2000
        max-attempts: 6

jwt:
  secret: 8db8d4987f6a51ed76090464c02e06205d68fc0b8d8f024e6c3c57cb67da7a9b
  issuer: seika-identity-service
  access-token-expiration-minutes: 60

logging:
  level:
    root: INFO
    org.springframework.cloud.config: DEBUG
    org.springframework.boot.context.config: DEBUG
    org.mongodb.driver: DEBUG
    org.springframework.data.mongodb: DEBUG
    com.seika.flashcard_service: DEBUG
